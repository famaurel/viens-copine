<div class="container">
  <h2 style='color:#3a1898; font-size:24px; font-weight:bold; padding: 2em 0em;'>Mes trajets créés</h2>

  <% unless @creator_bookings == nil %>
  <div class="container custom-card">
    <% @creator_bookings.each do |booking| %>
  </div>
  <% end %>
  <% end %>

  <div class="container custom-card">
  <% @creator_bookings.each do |booking| %>
  <a href="<%= my_booking_path(booking.id) %>" class>
    <div class="latte">
      <img src=<%= booking.user.photo %> class="avatar"/>
      <div class="latte-content">
        <p><%= booking.user.first_name %></p>
        <span class="small"> Départ : <%= Time.at(booking.trip.start_time).strftime("%Hh%M") %></span>
      </div>
      <div>
      </div>
        <div class="latte-info">
          <div class="stars">
           <%= link_to my_trip_path(booking.trip), method: :delete, data: { confirm: "Supprimez ce trajet" } do %>
            <i class="fas fa-times"></i>
           <% end %>
          </div>
          <i class="category <%= category_icon(booking.user.category) %>"></i>
        </div>
      </div>
    </div>
    </a>
    <% end %>
  </div>

  <h2 style='color:#3a1898; font-size:24px; font-weight:bold; padding: 2em 0em;'>Mes bookings à valider</h2>


  <% unless @pending_bookings == nil %>
  <div class="container custom-card">
    <% @pending_bookings.each do |pending_booking| %>

      <div class="latte">
        <img src=<%= pending_booking.user.photo %> class="avatar"/>
        <div class="latte-content">
          <p><%= pending_booking.user.first_name %></p>
          <span class="small"> Statut : <%= pending_booking.state %></span>
        </div>
        <div>
        </div>

        <div class="latte-info">
          <p data-pending_booking-button-id="<%= pending_booking.id %>"> <%= link_to "Approve", approve_my_booking_path(pending_booking), :method => :patch, remote: true, class: "btn btn-valid", style: "color:white" %></p>
        </div>
      </div>
    <% end %>
  </div>
  <% end %>




  <h2 style='color:#3a1898; font-size:24px; font-weight:bold; padding: 2em 0em;'>Mes trajets en cours et passés</h2>


  <% unless @client_bookings == nil %>
  <div class="container custom-card">
    <% @client_bookings.each do |client_booking| %>

      <div class="latte">
        <img src=<%= client_booking.user.photo %> class="avatar"/>
        <div class="latte-content">
          <p><%= client_booking.user.first_name %></p>
          <span class="small"> Départ : <%= Time.at(client_booking.trip.start_time).strftime("%Hh%M") %></span>
        </div>
        <div>
        </div>

        <div class="latte-info">
          <span class="small" style="color:#8898aa; font-size:16px;"> Arrivée : <%= client_booking.trip.end_address.title %></span>
        </div>
      </div>
    <% end %>
   <% end %>

  </div>
</div>



